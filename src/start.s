.section .text.init
.global _start

_start:
    # 🏆 這裡建議先手動寫一個 UART 輸出做為硬體冒煙測試
    li t0, 0x10000000
    li t1, 0x47       # 'G' 代表 Go
    sw t1, 0(t0)      # 直接在彙編階段印一個字元

    lui sp, 0x8       # 設定堆疊頂端於 0x00008000 [cite: 207, 239]
    jal ra, main      # 跳轉到 C [cite: 234]

loop:
    j loop
# 🏆 記得在這裡按一下 Enter 鍵，留一個空白行！
